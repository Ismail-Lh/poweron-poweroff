import styled from 'styled-components';

export const Wrapper = styled.div`
  width: 100%;
  height: 100%;
  display: grid;
  place-items: center;

  img {
    max-width: 30rem;
    z-index: 100;
    transition: all 0.2s ease-in;
  }
`;
